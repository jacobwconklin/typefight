import './SideBar.scss';

// SideBar
const SideBar = (props) => {
    
    // TODO one idea for sidebar is to let the user select the font of all the text on the site 

    return (
        <div className='SideBar'>
            
        </div>
    )
}

export default SideBar;